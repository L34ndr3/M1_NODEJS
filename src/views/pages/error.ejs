<%- include('../partials/header') %>

<div class="flex flex-col items-center justify-center min-h-[50vh] text-center">
    <h1 class="text-6xl font-bold text-red-500 mb-4">Oups !</h1>
    <p class="text-2xl text-slate-300 mb-8">Une erreur est survenue.</p>
    
    <% if (error) { %>
        <div class="bg-red-900/20 border border-red-500/50 p-4 rounded text-red-200 mb-8 max-w-lg">
            <%= error.message %>
        </div>
    <% } %>

    <a href="/" class="bg-blue-600 hover:bg-blue-500 text-white px-6 py-3 rounded-lg transition">
        Retour à l'accueil
    </a>
</div>

<%- include('../partials/footer') %>